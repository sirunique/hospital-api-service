A hospital is planning to migrate their locally available data and medical records into an API service.

Below is the Collection for the implemented API's



The requirements for the API service include:

Create An endpoint to add a new staff member to the service, a uuid is autogenerated for every staff member which will be required for subsequent API requests.
Staff profile comprise: id, name, uuid, registration_date



Create An endpoint to update staff member profile
(Requires validating staff uuid before access)



Create An endpoint to fetch all patient profiles (up to 2 years old)
Patient profile comprise: id, name, age, last_visit_date (Requires validating staff uuid before access)



Create An endpoint to download a specific patient’s profile into a csv file
(Requires validating staff uuid before access)



Create An endpoint to delete multiple patient profiles between a date range (Requires validating staff uuid before access)

